# Options-chain advanced math / pure runtime slice

- Repository: `bhagy774/jarvis-scalp`; source snapshot from `main` at `45c5d3b78436098fa36ea15cf39d35cd89259865` (`jarvis_FIXED.py`). PR #90 was queried directly and is **closed/merged** (merge time 2026-09-25T06:33:03Z); it is not an open PR to update. A separate PR was created for this follow-up.
- Runtime wiring: `jarvis_FIXED.py::Part14OptionsChain.analyze` is the production chain path. It already fetches selected-asset `get_institutional_bias` via Delta (BTC-only Deribit fallback) and then invokes `analyze_options_with_ollama`. Its pure-mode method returns explicit `WHALE_UNAVAILABLE` without stale tag/model invocation. Added deterministic descriptive telemetry there: finite positive observed support/resistance/max-pain levels are expressed as percentage distance from finite positive current price; missing/invalid observations produce `None` or fail-closed unavailable context. It retains the upstream bias score/signal and does not manufacture direction or use another asset's contracts.
- Optional model: inventory showed `jarvis_neural_cortex.py` but no clear trained weights/checkpoint artifact or provenance bundle in the repo tree. No neural inference/training was fabricated; trained numeric neural advisory remains unavailable absent a real vetted artifact and compatible input/provenance. Ollama stays only in explicitly non-pure mode; pure mode suppresses it.
- AI hedge guard: `options_hedged_scalp.py::execute_hedged_scalp` skips `evaluate_hedge_setup` in pure mode and defaults to no optional hedge; `_monitor_loop` skips AI hedge monitoring in pure mode. Existing paper futures leg, TP/SL, and position handling remain. This engine is separate from the Part14 analyzer rather than an options-analysis callsite; guarded it as a conservative adjacent runtime path.
- Tests: offline fixtures; `tests/test_options_math_integration.py` extracts actual Part14 production methods using AST, checks observed distances, invalid price fail-closed, and a model-call spy. `tests/test_options_ai_guard.py` checks pure mode advisor spy receives zero calls while paper futures path stays intact. Together with existing `tests/test_pure_options.py`: 5 tests passed; `py_compile` passed for modified runtime/module and related options modules. No live bot, orders, deployment, or merge.
- Exact paths: `jarvis_FIXED.py` (`Part14OptionsChain.analyze`, `analyze_options_with_ollama`); `options_hedged_scalp.py` (`execute_hedged_scalp`, `_monitor_loop`).
